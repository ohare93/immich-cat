{
    "name": "elm-app",
    "version": "0.1.0",
    "scripts": {
        "start": "elm-live src/Main.elm --start-page=src/index.html --host=0.0.0.0 --port=8000 -- --debug --output=dist/main.js",
        "build": "elm make src/Main.elm --output=dist/main.js"
    },
    "dependencies": {
        "elm": "^0.19.1",
        "elm-live": "^4.0.2"
    }
}
